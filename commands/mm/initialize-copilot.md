# initialize-copilot

Call the Memento `initialize-memento` MCP tool with ide="copilot". This creates `.github/prompts/` with GitHub Copilot slash-command files (recall, remember, wake-up, complete-project, sleep).
